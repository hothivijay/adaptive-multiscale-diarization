# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/

# Derived data and weights -- large and reproducible; keep them on persistent
# storage or attach them to a release, not in git history.
*.pt
*.ckpt
*.nemo
*.npy
*.npz
runs/
derived/
results/**/rttm/
results/**/weights/

# CORPUS DATA. DISPLACE 2024 is access-controlled and may not be redistributed.
# Never commit audio or reference annotation.
raw/
*.wav
*.flac
*.rttm

# Local
.DS_Store
.env
